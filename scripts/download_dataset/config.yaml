# Dataset download configuration
# Edit this file to specify which datasets to download

data_dir: "../../data/hugging_face"  # Relative to project root

datasets:
  - name: "di-zhang-fdu/AOPS"
    subset: null  # No config required